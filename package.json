{
  "name": "firestore-export",
  "version": "0.1.4",
  "description": "downloads a firestore collection to disk",
  "main": "./dist/index.js",
  "repository": "git@github.com:aredwood/firestore-export.git",
  "author": "Alex Redwood <hello@alexredwood.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc",
    "lint": "tslint --project .",
    "prepublish": "tsc && node prepare.js"
  },
  "files": [
    "dist"
  ],
  "nodemonConfig": {
    "ignore": [
      "export/*"
    ]
  },
  "devDependencies": {
    "@types/async": "^3.0.2"
  },
  "dependencies": {
    "@google-cloud/firestore": "^2.3.0",
    "async": "^3.1.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  }
}
